[project]
name = "auto"
version = "0.1.0"
description = "Automated nginx tunnel module protocol and memory tests"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aioquic>=1.3.0",
    "curl-cffi>=0.15.0",
    "h2>=4.3.0",
    "httpx[http2]>=0.28.1",
    "requests>=2.33.1",
]
